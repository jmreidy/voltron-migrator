var runsAsExpected = require('./migrator.js');

describe('Migrations with MySql', function () {
  context('when supplying mysql as the migrator type', function () {
    //runsAsExpected('mysql');
  });
});
